var React = require('react');
var Router = require('react-router');
var Link = Router.Link;

module.exports = React.createClass({
  render: function() {
    return (
      <div class="container">
        <div class="jumbotron" id="blogJumbo">
          <h1>The Blog portion of the site is coming soon!</h1>
        </div>
      </div>
    )
  }
})
